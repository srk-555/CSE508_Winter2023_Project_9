from django.http import HttpResponse, JsonResponse
from django.shortcuts import render


'''
def reviewData(request, productID):
    nutritionDetailsData = nutritionDetails.objects.get(id=productID)
    details = {
    "products": nutritionDetailsData
}

'''

